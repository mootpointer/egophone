%section
  %table
    %tr
      %th=value_header
      %th Count
    - stats.each do |stat|
      %tr
        %td{:class => ['Recipient', 'Sender'].include?(value_header) ? 'person' : ''}
          = stat['_id']
        %td= stat['value'][count_column].to_i